sylius_pdf_generation:
    default:
        adapter: dompdf
        storage:
            type: flysystem
            filesystem: pdf.storage
            prefix: pdf
            local_cache_directory: '%kernel.cache_dir%/pdf_local_cache'
